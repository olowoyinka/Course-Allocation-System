@using ReflectionIT.Mvc.Paging
@addTagHelper *, ReflectionIT.Mvc.Paging
@model PagingList<MappingLectureCourse.Models.MappingViewModel.ResearchArea>


@{
    ViewData["Title"] = "Index";
}

<h2>List of Research Area</h2>
<hr />

<div class="row">
    <div class="col-md-4 col-4"><a asp-action="Create" class="btn btn-primary">Add New Research Area</a></div>
    <div class="offset-3 col-md-5 col-4" style="float:right">
        <form asp-action="Index">
            <div class="row">
                <div class="col-md-6">
                    <input type="text" placeholder="Search ....." class="form-control" name="Search">
                </div>
            </div>
        </form>
    </div>
</div>

<hr />

<nav aria-label="Products navigation example">
    <vc:pager paging-list="@Model"></vc:pager>
</nav>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Update" asp-route-id="@item.ResearchAreaID">Update Session</a> |
                    <a asp-action="Detail" asp-route-id="@item.ResearchAreaID">View Detail</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<nav aria-label="Products navigation example">
    <vc:pager paging-list="@Model"></vc:pager>
</nav>
